using UnityEngine;

namespace Hackathon.WebPort
{
    public static class WebPortBootstrap
    {
        // Runtime auto-creation was intentionally removed. The WebPort scene must now be
        // authored through the WebPortGameManager inspector so every persistent object is
        // visible and editable before pressing Play.
    }
}
